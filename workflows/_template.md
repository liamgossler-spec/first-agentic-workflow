# Workflow: <name>

## Objective
What this workflow accomplishes, in one or two sentences.

## Inputs
- `input_1` — what it is, where it comes from, example value.

## Tools used
- `tools/<script>.py` — what it does and the args it takes.

## Steps
1. Read inputs and validate them.
2. Call `tools/<script>.py` with ...
3. Inspect output; on success continue, on failure see Edge cases.
4. Write the deliverable to <cloud destination> (Sheets/Slides/etc.).

## Expected output
What "done" looks like and where the deliverable lives.

## Edge cases & lessons learned
- Rate limits, timing quirks, retry strategy. Append here as you learn.
